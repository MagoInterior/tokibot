const google = require('google-it');

google({'query': 'Brasileirao'}).then(results => {

let modd = '🔎 「 preço de 1 mackBook 」 ────🔎'
					for (let V of mods) {
						 modd += ` Título: @${results.title}\n @${results.link}\n`
					}
					 modd += `╰──────*「 *Ashley bot* 」*────`

  console.log(modd)
}).catch(e => {
  console.log('erro');
})